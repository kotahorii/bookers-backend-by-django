export type FormInput = {
  username: string;
  password: string;
};
export type Cred = {
  username: string;
  password: string;
};
export type JWT = {
  refresh: string;
  access: string;
};
export type